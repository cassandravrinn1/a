# Unity YAML merge for scenes and prefabs
*.unity  text merge=unityyamlmerge eol=lf
*.prefab text merge=unityyamlmerge eol=lf

# Do NOT put all .asset into LFS (prevents LFS budget explosion)
# *.asset filter=lfs diff=lfs merge=lfs -text

# Large/binary source assets -> LFS
*.psd filter=lfs diff=lfs merge=lfs -text
*.fbx filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text

# Images (choose ONE strategy)

# Strategy A (keep as you have now; simple, but uses more LFS bandwidth):
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text

# Strategy B (recommended long-term): only track raw sources in LFS, keep png/jpg in Git
# *.png filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.tga filter=lfs diff=lfs merge=lfs -text

# Remove these overrides; they were only necessary because *.asset was in LFS
# Assets/Settings/Renderer2D.asset filter=lfs diff=lfs merge=lfs -text
# Assets/Settings/UniversalRP.asset filter=lfs diff=lfs merge=lfs -text
